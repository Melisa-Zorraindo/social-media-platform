import { fecthProfilePosts } from "../auth/commonFunctions/api.mjs";

const key = localStorage.getItem("accessToken");
// const username = localStorage.getItem("username");

/* if (key) {
  const PROFILE_POSTS = await fecthProfilePosts(key);
  const PROFILE_POSTS_CONTAINER = document.querySelector(
    "#profile-posts-container"
  );
} */
